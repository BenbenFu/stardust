# 卡片 style_json 手册索引

两本手册，按需取用：

| 手册 | 读它当… | 内容 |
|---|---|---|
| **[拼装手册.md](./拼装手册.md)** | 你只需「从现有组件挑 → 拼 style_json」 | 原理 / schema / 组件 value 清单 / 工作流 / 存 STYLE_POOL / 约束 / 示例。**不含任何 CSS、SQL、入库细节。** |
| **[元素入库手册.md](./元素入库手册.md)** | 你要设计新元素或改现有组件 CSS | 14 槽画布机制、三类实现方式、标准 CSS 模板、锚点定位、兼容性、常见反模式（含 starfield 案例）、DB id 索引、SQL 模板、验证流程。 |

> **拼装时若发现现有组件与主题明显不搭，不要硬凑**——直接切到《元素入库手册》提新元素请求。
> 配套修复 SQL（注意版本状态）：
> - `starfield_fix_20260724.sql` —— ✅ 已落地。修 id=48 星空背景的 14 槽契约违规（写死 none → 全 var()；DB 现状已是修复版，属历史修复）。
> - `element_floating_circle_deco_layer_20260725.sql` —— ✅ 已落地（现行修复）。floating_circle(id=34) 脱离 14 槽画布、隔离到 `.card-deco-layer::before`，读 `--el-float-pos-*`，与 bg 画布正交，根治"星空 × 浮动圆"层叠争用。详见《元素入库手册》§8 案例 7 / 时间线。
> - `element_revert_reorder_20260725.sql` —— ⚠️ 回退用。若误执行了下面两份作废 SQL，用此回退到 reorder 前。
> - ~~`element_composite_reorder_20260725.sql`~~ / ~~`element_strip_composite_20260725.sql`~~ —— ❌ **已作废**（2026-07-25 那轮"主合成收归引擎/重排图层"实测"验证页几乎不显示"已整体回退，引擎回退 `20260724a`、DB 回退到 reorder 前）。两份均已移至 `_deprecated/`，**不要再执行/参考**。当前架构以《元素入库手册》§2.3 / §4.1 为准：每个背景类模板自带逐字一致 14 槽主合成块，而非引擎集中。
