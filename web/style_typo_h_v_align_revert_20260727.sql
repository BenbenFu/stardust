-- ============================================================
-- 回退：删除 h_align / v_align（字段级双轴对齐重构）新增的行
-- 仅删除本重构插入的 sub_dim 行；不影响其它维度。
-- 引擎需同步回退到 20260726b（撤销 writing-mode 移到文字元素、撤销 h/v_align 维度）。
-- ============================================================

DELETE FROM style_typo_options WHERE sub_dim IN ('h_align','v_align');
